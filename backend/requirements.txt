aniso8601==10.0.1
attrs==25.4.0
blinker==1.9.0
certifi==2026.1.4
charset-normalizer==3.4.4
click==8.3.1
colorama==0.4.6
cors==1.0.1
filelock==3.20.3## Pull Request: Initial Project Setup for Minerva Marketplace

### Description
Set up the foundational structure for a Flask + React web application with proper configuration and development environment.

### Changes Made

#### Project Structure
- Created `/backend` directory for Flask API
- Created `/frontend` directory with React app (via create-react-app)

#### Frontend Setup
- Initialized React app with create-react-app
- Installed Tailwind CSS for styling

#### Configuration
- Created `/backend/requirements.txt` for Python dependency management
- Created comprehensive `.gitignore` with entries for:
  - Python: `__pycache__/`, virtual environments, `.egg-info/`, etc.
  - Node.js: `node_modules/`, npm logs
  - Environment files: `.env` and `.env.local`
  - IDE files: `.vscode/`, `.idea/`
  - OS-specific files: `.DS_Store`

### Next Steps
- [ ] Implement Flask backend with initial API routes
- [ ] Set up database models and ORM
- [ ] Configure CORS for frontend-backend communication
- [ ] Build React components and pages
- [ ] Add authentication/authorization

### How to Use
```bash
# Backend setup
cd backend
pip install -r requirements.txt

# Frontend setup
cd frontend
npm install
npm start## Pull Request: Initial Project Setup for Minerva Marketplace

### Description
Set up the foundational structure for a Flask + React web application with proper configuration and development environment.

### Changes Made

#### Project Structure
- Created `/backend` directory for Flask API
- Created `/frontend` directory with React app (via create-react-app)

#### Frontend Setup
- Initialized React app with create-react-app
- Installed Tailwind CSS for styling

#### Configuration
- Created `/backend/requirements.txt` for Python dependency management
- Created comprehensive `.gitignore` with entries for:
  - Python: `__pycache__/`, virtual environments, `.egg-info/`, etc.
  - Node.js: `node_modules/`, npm logs
  - Environment files: `.env` and `.env.local`
  - IDE files: `.vscode/`, `.idea/`
  - OS-specific files: `.DS_Store`

### Next Steps
- [ ] Implement Flask backend with initial API routes
- [ ] Set up database models and ORM
- [ ] Configure CORS for frontend-backend communication
- [ ] Build React components and pages
- [ ] Add authentication/authorization

### How to Use
```bash
# Backend setup
cd backend
pip install -r requirements.txt

# Frontend setup
cd frontend
npm install
npm start
Flask==3.1.2
flask-cors==5.0.1
flask-restx==1.3.2
future==1.0.0
gevent==25.9.1
greenlet==3.3.1
idna==3.11
importlib_resources==6.5.2
itsdangerous==2.2.0
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
MarkupSafe==3.0.3
PySocks==1.7.1
referencing==0.37.0
requests==2.32.5
requests-file==3.0.1
rpds-py==0.30.0
tldextract==5.3.1
urllib3==2.6.3
Werkzeug==3.1.5
zope.event==6.1
zope.interface==8.2
